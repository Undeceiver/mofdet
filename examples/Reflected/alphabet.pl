:- multifile ground_var/2.
% This file should be generated from the ontology.

% Variables
% Variables are indicated as ground_var(var_name, var_term)
ground_var(v1).
ground_var(v2).

% Functions (inc. constants)
% Functions are indicated as ground_func(name,arity).
ground_func(constant,0).

% Predicates
% Predicates are indicated as ground_pred(name,arity).
% ground_pred(p,1).
ground_pred(pizza_topping,1).
ground_pred(edam_topping,1).
ground_pred(mozzarella_topping,1).
ground_pred(cheddar_topping,1).
ground_pred(parmezan_topping,1).
ground_pred(pizza,1).
ground_pred(has_topping,2).
